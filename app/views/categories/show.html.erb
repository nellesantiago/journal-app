<p>
  <strong>Title:</strong>
  <%= @category.title %>
</p>
<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Delete', @category, method: :delete, data: { confirm: "Are you sure you want to delete this category? This can't be undone." } %> | 
<%= link_to 'Back', categories_path %>
<%= render "tasks/index", tasks: @tasks, category: @category %>
